{
    "files.exclude": {
        "node_modules/": true,
        "**/*.js": { "when": "$(basename).ts" },
        "**/*.d.ts": { "when": "$(basename).ts" },
        "lib/": true
    },
    "typescript.tsdk": "./node_modules/typescript/lib"
}